# DRM Contracts

Deterministic WASM contracts funded exclusively with DRM (`asset_id=1`). Deploy and call through the `deploy_contract` and `call_contract` RPCs; execution is metered by the fixed-cost gas model in `sidechain/wasm/gas`.
